import 'package:flutter/material.dart';

class ConstrainedBox extends StatelessWidget {
  const ConstrainedBox({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ConstrainedBox(

    );
  }
}
